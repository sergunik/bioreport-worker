services:
  worker:
    build:
      context: .
      dockerfile: docker/worker/Dockerfile.dev
    env_file: .env
    volumes:
      - .:/app
      - ./files:/app/files:ro
    networks:
      - bioreport

networks:
  bioreport:
    driver: bridge

volumes:
  postgres_data:
